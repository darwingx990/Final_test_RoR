<%= form_with url: request.path, method: :get, local: true do |f| %>
  <%= f.select :locale,
    [["us English", "en"], ["🇨🇴 Español", "es"]],
    { selected: I18n.locale },
    { onchange: "this.form.submit()", class: "bg-gray-100 border rounded p-2" } %>
<% end %>

<p>
  <strong><%= t('images.show.tittle') %>:</strong>
  <%= @image.tittle %>
</p>
<p>
  <strong><%= t('images.show.description') %>:</strong>
  <%= @image.description %>
</p>

<% if @image.image.attached? %>
  <%= image_tag url_for(@image.image) %>
<% end %>

<%= link_to t('images.show.edit'), edit_image_path(@image) %> |
<%= link_to t('images.show.back'), images_path %>
